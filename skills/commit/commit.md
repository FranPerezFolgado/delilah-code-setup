---
description: Generates a Conventional Commits message from staged changes and branch name
---

# Commit

Delegate to the `commit-writer` subagent.

The agent will review the staged diff and generate the message. Returns only the message, ready to use.
